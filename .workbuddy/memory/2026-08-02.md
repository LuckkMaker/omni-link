# 2026-08-02

## monitor 波形采样率优化调研（对比 plink）

- 核实了 https://github.com/pushk3n/plink（pyOCD 直连 DAP 读内存画波形，PyQt6+pyqtgraph）
- **4500Hz 说法属实**：commit e2d43cd "针对 dap-link V2 bulk升级, 10MHz仅采样模式下达到4500+Hz"，条件是 10MHz SWD + DAPLink V2 bulk + "仅采样"（关闭绘图跑纯采样循环，非 UI 模式开关）
- plink 核心优化手段：
  1. SWD 时钟默认 8MHz（omni 当前 1MHz，差 8-10 倍，是最大瓶颈）
  2. 跨 Block 流水线读取 `_pipelined_batch_read()`：直接操作 AP/DP（dp.write_ap + read_ap_multiple + now=False + dp.flush()），多 Block 命令一次发出，Bulk V2 pipeline_depth=6+
  3. Block 合并激进：merge_gap=65536（64KB），max_bytes: HID 1024B / Bulk 2048B，最小载荷比 20%
  4. 读取计划缓存 `_get_read_plan()`（omni 已有 vars_cache 缓存分组）
  5. `enable_read_cache=False` 保证实时性
  6. busy-wait 高精度节流 `_busy_wait_until`（omni 用 time.sleep，精度差）
  7. 热路径零内存分配：预编译 struct.Struct + numpy ring buffer
  8. 数据全进程内，无 WS/JSON 序列化（omni 有 WS+JSON 开销）
- omni 现有差距（文件级）：
  - pyocd_backend.py:176 `actual_speed = speed if speed else 1000000`（1MHz）
  - monitor_backend.py:41 `MERGE_GAP = 16`（plink 64KB）
  - monitor_backend.py `_read_variables` 逐 group 串行 read_memory_block32，无跨 block 流水线
  - monitor_backend.py:489 `PUSH_BATCH = 8` 固定，WS 消息数 = rate/8
  - monitor.store.ts:212 zustand `[...s.samples, ...pts]` 全量拷贝
- 建议优化优先级：① SWD 时钟提到 4-8MHz 并暴露前端 speed ② 借鉴流水线批量读（AP/DP 层）③ MERGE_GAP 放大+载荷比保护 ④ PUSH_BATCH 动态化/负载减重 ⑤ busy-wait 节流

## 影响面评估（用户问是否影响其他页面）

- **连接全局共享**：所有功能（monitor/flash/commander/RTT/SWV）共用同一 pyOCD session（`backend._get_session(uid)`），SWD 时钟改动是全局的，无法只对 monitor 提频
- **前端 speed 链路已通**：probe.store.ts:103 `pendingSpeed: 1_000_000`（默认 1MHz），probe.service.ts 已传 options.speed，只是 UI 未暴露控件 → P0 只需加 UI 控件+调整默认值，后端 0 改动
- **monitor_backend 公开接口跨页共享**：flash.py/commander.py 用 `pause_during(uid)`（烧录/读写时暂停采样）；rtt_backend.py 用 `is_running`（RTT 与 monitor 互斥）；probe_monitor.py 用 `on_probe_disconnected`；server.py 用 `cleanup_all`
- **影响结论**：P0 全局影响（性能提升为主，风险=高时钟下探针/线缆稳定性，缓解=保留默认+可配置+失败降频重试，pyocd 有 fail_safe）；P1/P2/P3 全封闭在 monitor 私有实现（_read_variables/_build_read_groups/_emit_samples），保持公开方法签名不变即不影响其他页面；P2 若改 payload 结构需前后端契约同步（只影响 monitor 生态）；流水线读必须带 fallback 回退 read_memory_block32（plink 同款做法）

## P1 已实现（用户确认设备连接页已有速度选择，只做 P1）

改 `python/core/monitor_backend.py`（仅私有实现，公开接口签名不变，不影响 flash/commander/RTT 等页面）：
1. **跨 Block 流水线读取 `_pipelined_read`**：直接操作 AP/DP 层（dp.write_ap + read_ap_multiple(now=False) + 一次 dp.flush()），所有 Block 的 DAP 命令一次 USB 往返发出；**按 MEM-AP auto_increment 页边界（默认 1KB）拆子页**（plink 未处理此边界，已修正其缺陷）；CSW 设 32 位传输且不恢复（pyocd _cached_csw 机制下是正常状态，与 _read_block32_page 行为一致）
2. **Block 合并策略激进化**：MERGE_GAP 16B→64KB、max_bytes 按传输类型（bulk 2048 / hid 1024 / unknown 512）、MIN_PAYLOAD_RATIO=0.20 兜底防冗余、start/length 强制 4 字节对齐
3. **`_get_transport_type(uid)`**：从 session.probe._link._interface.is_bulk 检测传输类型（带缓存，断开时清理）
4. **fallback**：流水线任何异常回退到原串行 read_memory_block32/block8 路径
- 新增离线测试：python/test_read_groups.py（合并/对齐/载荷比/max_bytes）+ python/test_pipelined_read.py（流水线/回退/跨页，mock AP/DP 层），全 PASS，可作回归保障
- pyocd vendored 版本确认：dap.py write_ap 无 now 参数（deferred 模式自动缓冲，无需）；probe lock 是 RLock（多 deferred 排队安全）；常量在 pyocd/coresight/ap.py

## 卡顿修复（用户反馈 P1 后波形刷新卡顿）

- 根因排查：unlock() 不 flush（排除流水线退化串行）；CSW 绕过写入无污染（pyocd write_reg 有 _cached_csw 比较，后续操作会写回正确值，ap.py:1038-1050）；最可疑 = 流水线在真实探针上失败 → 每帧"尝试+整批串行重读"双倍开销 → 采样率暴跌
- 修复（monitor_backend.py）：
  1. **仅 bulk 探针启用流水线**：`_get_transport_type(uid) != "bulk"` 直接走串行（HID pipeline_depth=1 无收益只有风险，plink 注释同款判断）
  2. **失败一次即降级** `_pipeline_disabled[uid]=True`：后续直接串行不再重试（防双倍读取）；start() 重置（新会话给一次机会）；on_probe_disconnected 清理
  3. 失败时 logger.warning 输出 "Monitor pipelined read failed, disable pipelining for probe ..."，便于用户诊断
- 测试补充用例7(HID走串行)/8(降级)/9(start重置)，全 PASS
- 注意：若用户 bulk 探针且流水线成功，卡顿可能来自采样率升高后前端 WS/渲染压力 → 走 P2（PUSH_BATCH 动态化/负载减重）

## 流水线失败根因修复（用户贴日志确认）

- **日志证据**：`WARNING: Monitor pipelined read failed ... 'Apm32f407ig' object has no attribute 'ap'`（3 条 = 3 次采样启动，降级机制正常）
- **根因**：项目自带 APM32F407IG target（python/pyocd/target/builtin/geehy/target_APM32F407xx.py）继承 **CoreSightTarget 而非 CortexM**，没有 `.ap` 属性（CortexM 的 `.ap` 来自构造参数 CoreSightCoreComponent.__init__）；CoreSightTarget 暴露的是 `.dp` + `.aps` + **`.first_ap`**（coresight_target.py:354-358，排序后第一个 AP，单核=AHB-AP#0）
- **修复**：`_pipelined_read` 里 `ap = getattr(target,'ap',None) or getattr(target,'first_ap',None)`，两者皆无才抛 AttributeError → 触发降级
- 测试用例10（模拟无 .ap 有 .first_ap 的 target）全 PASS
- 结论：修复后 bulk 探针（DAPLink V2/WinUSB）流水线才真正启用；HID 探针按设计走串行（plink 同判据），性能也不低于改动前（串行同样吃到 64KB 合并+对齐）
- **用户澄清 + 机制确认**：项目实际未用内置 target_APM32F407xx.py，target 从 DFP 动态导入。pyocd 机制（pack_target.py:535）：`type(subclassName, (superklass,), {__init__: _pack_target__init__})`，无 family 匹配时 superklass=CoreSightTarget，`_pack_target__init__`（433）调 `super().__init__` → CoreSightTarget.__init__ → **有 .dp/.aps/.first_ap，无 .ap**（与报错吻合）。`.first_ap` 是 CoreSightTarget property（coresight_target.py:354），pyocd 内部 pack_target.py:302 也用 `self._target.first_ap` → 上一轮的 ap→first_ap 回退修复对 DFP target 同样有效，无需再改
- **用户新日志确认修复生效**：08:19:45 启动，无 "pipelined read failed" warning
- **文档**：写入 docs/tech/monitor-performance.md（完整方案：瓶颈分析/plink核实/流水线/合并策略/降级机制/4个踩坑/影响面/验证/后续方向），并在 mkdocs.yml nav 增加"技术参考"节（含现有 tech/*.md 4篇）

## 采样率实测数据与节流优化（用户测试固件 dsp_wave）

- **用户实测**：设定 1kHz → actual_rate_hz ≈ 500+；设定 5kHz → ≈ 1kHz
- **解读**：① ~1kHz 是读取硬上限（单帧 SWD 读取 ~1ms，5kHz 也到不了更高）② 1kHz 设定只剩 500 是因为 **Windows 上 time.sleep 最小精度 ~1ms**，`sleep(interval-elapsed)` 每帧多撑 ~1ms
- **已改**（monitor_backend.py `_sample_loop` 节流）：混合节流 = sleep_time>2ms 先 `time.sleep(sleep_time-0.001)` 再忙等 `while monotonic()-t0 < interval: pass`（monotonic 与 t0 同源，不能用 perf_counter 混算）；消除 sleep 精度损耗
- **已改**：start() 启动日志加 `probe_transport=bulk/hid`，便于确认流水线是否启用（bulk 才走流水线）
- **待验证**：节流优化后 1kHz 设定应回升到接近 ~1kHz 硬上限；要突破 1kHz 需降低读取耗时 → P0（连接页提高 SWD 时钟，用户已有速度选项）或确认探针为 bulk（流水线生效）

## 64Hz 回退根因（实测确认）+ monitor 页面日志区

- **用户探针确认**：CMSIS-DAP v2 (WinUSB bulk), packet_size=64, packet_count=4, deferred=True（flash log 区显示）→ 流水线应启用；用户 SWD 10MHz 测试
- **64Hz 根因（实测）**：项目 .venv 是 **Python 3.12.0**，其 `time.monotonic()` 在 Windows 上 **resolution=0.015625（15.6ms）**（3.13 才是 QPC 1e-07）；忙等循环 `while monotonic()-t0 < interval` 每帧卡在 15.6ms 刻度 → 64Hz。而 `time.perf_counter()` 是 QPC（1e-07，实测精确）
- **修复**：monitor_backend.py 全部 `time.monotonic()` → `time.perf_counter()`（时间戳都是差值运算，语义等价；含 start/pause/resume/统计/节流/忙等），venv 环境回归全 PASS
- **前端 monitor 页面日志区**（仿 flash）：
  - monitor.store.ts 加 `logs: LogEvent[]` + `addLog`（保留500条）+ `clearLogs`（reset 一并清）
  - useProbeWs.ts 日志分发：含 'RTT'→rtt store、含 'Monitor'→monitor store、否则→flash store
  - monitor/index.tsx 底部加 ResizeHandle（双击折叠/展开，记 lastExpandedHeight）+ LogConsole（共享组件）
  - 后端：流水线降级 warning 同时 `event_manager.log("warning", ...)` 发前端（logger.warning 只到终端，event_manager.log 才进页面 log 区）
- **log 去向说明**：`logger.*`（Python logging）→ 开发终端 stderr；`event_manager.log` → WS 'log' 事件 → 前端 log 区
- typecheck:web 有 4 个既有错误（InfoPanel formatKb / ChannelPanel Set 迭代 / flash.store showReloadDialog），均非本次改动引入

## 日志区改全局 + 筛选 + 边栏避让（用户需求）

- **后端**（python/core/events.py）：`log()` 事件加 `source` 字段，由 `_infer_log_source(message)` 集中推断（含 Monitor→monitor / RTT→rtt / Commander→commander / Flash·烧录·擦除等→flash / 其他→system），调用方零改动；已单测 6 条 PASS
- **全局 store**（src/stores/log.store.ts 新建）：logs（保留 1000 条）+ filter（all/monitor/flash/rtt/commander/system）+ rightInset（右侧边栏避让宽度）+ addLog（后端 source 缺失时按消息前缀兜底推断，规则与后端一致）
- **组件**（src/components/GlobalLogConsole.tsx 新建）：GlobalLogConsole（来源筛选下拉带计数 + 复用 LogConsole）+ GlobalLogArea（按 rightInset 让出右侧宽度）
- **挂载**（src/layouts/MainLayout.tsx）：main 改 `flex-col`，页面区（flex-1 overflow-auto 包 Outlet/Commander keep-alive）+ ResizeHandle（双击折叠/恢复，记上次高度）+ 底部 GlobalLogArea
- **路由**（src/hooks/useProbeWs.ts）：log 事件统一 → useLogStore.addLog（不再分流到 flash/rtt/monitor store）
- **useRttSession.ts**：RTT 会话状态日志改发全局 log store（logEvent）
- **边栏避让**：monitor（ChannelPanel 宽 sidebarWidth）、rtt（ConfigPanel 宽 sidebarWidth）在边栏宽度变化时 `setRightInset(sidebarWidth)`，卸载时归 0；无右侧边栏页面（flash/commander/tools）日志区全宽
- **移除各页面本地日志区**：monitor（回退上轮新增）、flash（bottomHeight/ResizeHandle/FlashLogConsole 全删）、rtt（底部日志区删）；monitor.store 回退 logs/addLog/clearLogs
- **遗留**：flash.store（logs/onLog/clearLogs）、rtt.store（logs/addLog/clearLogs）成死代码（无 UI/调用方），暂留待后续清理；typecheck 仍只有 4 个既有错误

## 修复：StatusBar 实际采样率显示 + 右侧边栏避让（用户反馈）

- **StatusBar**（src/components/layout/StatusBar.tsx）：实际采样率 `(实际 xxx)` 原先仅在 `actual < rate*0.8` 时显示（阈值条件），优化后实际接近设定就不显示了 → 去掉阈值，**始终显示**；低于 80% 时琥珀色警示，否则白色/60
- **右侧边栏避让补齐**：
  - 漏了 **commander 页面**（CommandSidebar，宽 sidebarWidth，与 monitor/rtt 同款）→ 已加同步
  - **keep-alive 竞态**：commander 是 keep-alive（切走不卸载），原"卸载 cleanup 归 0"不适用，且 monitor 挂载与 commander 归 0 的 effect 顺序不确定会互相覆盖
  - **重构为多来源聚合**（log.store）：`insets: Record<source, width>` + `setRightInset(source, width)`，日志区避让宽度 `selectRightInset = max(values)`；monitor/rtt/commander 各写自己的 key（commander 用 useLocation 感知激活，非激活置 0）→ 无竞态
  - flash/settings/tools 确认无右侧边栏，日志区自动全宽
- typecheck：仍仅 4 个既有错误，本轮零新增

## 日志区放弃右侧边栏避让（用户反馈避让不生效，直接不避让）

- **原因**：用户实测 log 区仍未能避让右侧边栏，要求直接不避让
- **改动**：移除全部避让逻辑 —— log.store 删 insets/setRightInset/selectRightInset；GlobalLogArea 删 marginRight（日志区全宽）；monitor/rtt/commander 三个页面删 setRightInset effect 及 useLogStore/useLocation import
- 全局日志区现在恒为全宽，逻辑更简单；typecheck 仍只有 4 个既有错误

## 采样率实测反馈 + 前端渲染卡顿修复（1kHz达标，5kHz→2.2kHz，波形一段段）

- **用户实测**：1kHz 设定 → actual≈1kHz ✓（perf_counter 修复生效）；5kHz → ~2.2kHz（单帧 ~0.45ms，pyocd 调用链+USB 往返硬开销）；波形"一段一段"绘制
- **前端根因**：`appendSamples` 每 8 点一次 `[...samples, ...pts]` 全量拷贝（2.2kHz → 275 次 set/s × 数组长至 10 万）→ 主线程 GC/拷贝风暴 → 渲染卡顿一段段
- **修复（monitor.store.ts 重构）**：
  - 模块级 `samplesBuffer`（引用稳定可变数组，消除全量拷贝）+ `pendingSamples` 待提交队列 + **16ms 分帧批量 flush**（set 频率 275/s → ~60/s）
  - 新增 `samplesVersion` 字段：每次 flush +1，驱动订阅者重渲染（samples 引用不变，须依赖 version）
  - 订阅方全部加 version：monitor/index.tsx（页面）、WatchPanel、ChannelPanel、**WaveformChart（新增 samplesVersion prop，渲染调度 effect 依赖 [samples, samplesVersion]）**——否则波形不重绘
  - clearSamples/reset：清 buffer+pending+定时器
- **后端小优化（monitor_backend.py）**：
  - `push_batch = max(8, rate//50)` 动态批量推送（5kHz → 100 msg/s，1kHz → 20 msg/s）
  - `_pipelined_read` CSW 写入缓存 `_pipelined_csw[id(ap)]`（CSW 不变则跳过写，省 AP 写+DP SELECT）；start() 时清空（防外部改 CSW 后缓存失效）
- 验证：后端测试 ALL PASS；typecheck 仅 4 个既有错误
- **未解决**：5kHz 只能到 ~2.2kHz（单帧 pyocd 调用+USB 往返 ~0.45ms 是当前上限；plink 4500Hz 是"仅采样"模式无 Web/WS 开销）。如需再提，可考虑批量合并 WS payload 为二进制、或深入 pyocd 调用链优化

## 修复：ELF 数组变量不显示元素选择（用真实固件 omni.axf 定位）

- **根因 1（DWARF char 编码漏判）**：`_base_type_to_monitor` 只认 DW_ATE_signed(0x05)/unsigned(0x07)/float(0x04)，漏了 **DW_ATE_signed_char(0x06)/unsigned_char(0x08)**——GCC 对 char/uint8_t 数组（如 AHBPrescTable uint8_t[16]）用 0x08，导致所有 char 数组类型解析失败（_resolve_var_type 返回 None）→ get_symbols 走 fallback is_array=False → 前端无展开箭头。**已修**：补 0x06→int8、0x08→uint8
- **根因 2（无 DWARF 的库常量表）**：C/S/pio2s/twooverpi（CMSIS-DSP 库，40-156B）无调试信息（库编译不带 -g）→ 无法识别。**已加保守 fallback**：STT_OBJECT size>=16 且 %4==0 → 猜 uint32[n] 数组，**is_guess=True 标记**（size=8 不猜，避免 struct/double/函数指针误判，init_func 已排除）
- **验证**：真实解析 omni.axf → 5 个数组全部识别（AHBPrescTable uint8[16] 精确 + 4 个 guess），init_func(8B) 不再误判
- **前端**：MonitorSymbol 加 is_guess?: boolean；ChannelPanel 数组名后显示琥珀色 *（tooltip 提示"数组类型为猜测，无调试信息"）
- 后端测试 ALL PASS；typecheck 仅 4 个既有错误
- 排查方法复用：mock core 依赖 + 直接读用户固件 ELF 调 _build_dwarf_cache/get_symbols 验证（无需硬件/后端运行）

## 采样率 5kHz→2.8kHz 后下一步：probe 层直连优化

- **瓶颈分析**：单帧 ~0.36ms 中 USB 往返（DAPLink 单事务 ~0.2-0.3ms）是硬性大头；pyocd dp 层每帧多付 2 次 _select_ap（lock + DP SELECT 命令 + TRACE）是次要可压部分
- **已做（monitor_backend._pipelined_read）**：probe 层直连——单 AP 目标（dp.aps<=1，如 DAPLink+单核 MCU）在 CSW 首次设置（走 dp 确保 APSEL + flush 立即生效）后，TAR/DRW/flush 改用 `dp.probe`（CmsisDapProbe）直连，跳过 dp._select_ap；多 AP 目标保守走 dp（probe 不管理 APSEL）
- **可行性确认**：cmsis_dap_probe.A32=0x0000000c，REG_ADDR_TO_ID_MAP 只取 AP 寄存器 2 位偏移（CSW/TAR/DRW=0x0/0x4/0xC），传完整 AP 地址（含 APSEL）可正确映射，无 KeyError
- **修复**：空分组（total_words<=0）时直接返回不写 CSW（用例5 暴露）
- 测试扩到 12 用例（probe 直连/多 AP 走 dp/空分组），全 PASS
- **预期**：小幅提升（2.8→3.0kHz 左右）；软件侧已接近 DAPLink 极限；更高需换高性能探针（ST-Link V3/J-Link/高速 CMSIS-DAP V2）或接受现状

## WS payload 结构减重（用户选择"结构减重"方案）

- **后端** `_emit_samples`：monitor.sample 由 `{uid, samples:[{t_ms, values:[{id,value}]}]}` 改为 `{uid, ids:[...], t:[...], v:[[...]]}`（ids 批次内只发一次，JSON 体积减半，序列化更快）；变量中途增删按 id 序列拆子批次保证 ids 一致
- **前端** monitor/index.tsx offSample：解析新格式展开为 SamplePoint[]，**向后兼容旧格式**（samples 字段优先）
- **验证**：test_pipelined_read 扩到 13 用例（含结构减重/拆批）全 PASS；typecheck 仅 4 个既有错误
- **文档**：docs/tech/monitor-performance.md 更新"已实施优化"章节（PUSH_BATCH 动态/payload 减重/busy-wait/分帧提交/probe 直连），并记录当前瓶颈=USB 往返（DAPLink 极限 ~3kHz）+ 硬件建议
- **预期**：采样率小幅提升（~5-10%），主要降低前端解析压力与 WS 带宽

## 波形渲染毛刺修复（用户反馈：采样越久越毛刺 + 后续采样影响已绘波形）

- **根因**：WaveformChart 自研 min/max 降采样**按数据索引分桶**（bucketSize = ceil(总点数/20000)），总点数增长时 bucketSize 取整变化 → 所有桶边界整体漂移 → 重绘时同一位置数据被分到不同桶，min/max 尖峰位置乱跳 → 毛刺 + "后续采样改变已绘波形"
- **修复（WaveformChart.tsx）**：
  1. **去掉自研索引分桶降采样**：buildPlotData 全量透传（rows 转置），由 **uPlot 1.6 内置像素桶 decimation**（可视窗口点数 ≥ 4×像素宽时自动 min/max，桶基于像素列稳定无漂移）负责降采样——uPlot 运行时自动触发，**无需配置**（d.ts 无 decimation 字段，1.6 是硬编码自动逻辑，esm.js:2167）
  2. **滑动平均改前缀和 O(n)**（全量透传后 O(n*w) 会卡）：preSum+preCnt 前缀数组，O(1) 查窗口和；node 验证与旧实现完全一致（maxDiff 1e-15），10 万点 7.9ms→6.1ms
  3. 删除 MAX_RENDER_POINTS 常量
- **预期**：问题2 根治（桶稳定）；问题1 大幅改善（尖峰位置稳定）；若数据本身有 SWD 读错毛刺，用滑动平均平滑
- **性能注意**：全量 10 万点 setData + uPlot decimation 由 uPlot 处理（官方支持百万点）；多变量时 buildPlotData 全量遍历开销线性增长，如需可降 MAX_SAMPLES（当前 10 万）

## 波形毛刺排查（4MHz 下仍存在 → 禁用 probe 直连二分定位）+ CSV 时间列

- **用户反馈**：4MHz SWD 下毛刺依旧，且指出"是上次采样率优化引入的"；新 CSV（4MHz, 985Hz 采样）分析：毛刺 = 连续 2 帧精确 0.000，前后帧正常（80.9→0→80.9），时间戳完全单调，间隔 ~3s（正弦周期整数倍）——**与 10MHz CSV 特征完全一致**，排除 SWD 时钟超频
- **判定**：后端 RingBuffer 的 0 只能来自 _read_variables 读取层；上次优化唯一改动读取路径的是 **probe 层直连**（单 AP 跳过 dp._select_ap）→ 嫌疑最大（probe 直连不管理 DP SELECT/APSEL，pyocd 内部某些时机 SELECT 状态变化时读到错 AP 的 DRW = 0）
- **已做**：_pipelined_read 临时禁用 probe 直连（accessor = dp），保留注释；测试用例11 改为验证走 dp；全部 PASS
- **待验证**：用户重测毛刺是否消失（消失=probe 直连 bug，仍在=继续查 CSW 缓存/PUSH_BATCH）
- **CSV 时间**：export_csv 增加 time 列（采样启动系统时间 start_wall + t_ms，格式 HH:MM:SS.mmm），表头 t_ms,time,var；start() 记录 _start_wall = time.time()；已验证

## probe 直连彻底移除（验证闭环）+ CSV 文件名时区修复

- **验证（用户新 CSV monitor_2026-08-02-03-06-33.csv，4MHz/5kHz设定）**：36744 点，**毛刺 0、精确0帧 0**，时间戳单调，值范围 -100~100 完整正弦 → probe 直连禁用后毛刺消失，确认其是毛刺根因（probe 层不管理 DP SELECT/APSEL，pyocd 内部 SELECT 状态变化时读到错 AP 的 DRW=0）
- **采样率无损失**：2831Hz 与禁用前一致 → probe 直连省掉的 dp._select_ap Python 开销对帧时影响可忽略（USB 往返占大头），无保留价值
- **已彻底移除**（非临时禁用）：_pipelined_read 删除 accessor 选择逻辑统一走 dp，docstring 记录弃用原因；测试用例11 文案更新，13 用例全 PASS
- **CSV 文件名时间戳 bug**：前端 monitor/index.tsx 用 `new Date().toISOString()`（**UTC**）生成文件名 → 比本地时间少 8 小时（03:06 vs 11:06）；已改本地时间组件拼接（YYYY-MM-DD-HH-MM-SS）；LogConsole.tsx（全局日志区保存）同坑一并修；flash/components/LogConsole.tsx 为无引用死代码未动
- **CSV 内容验证**：新格式表头 [t_ms, time, var]，time 列已是本地时间（上轮修复生效）——注意解析脚本要取 r[2] 为值（r[1] 是时间字符串）
- 前端 typecheck 零新增错误（仅剩 4 个既有）
