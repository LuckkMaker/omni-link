# 2026-08-03 工作日志

## Number Converter 页面优化：增加左移 / 右移位运算
- 文件：`src/pages/tools/number-converter.tsx`
- 新增「位运算」卡片（位于错误提示与 32-bit 可视化之间）：
  - 移位位数输入框（0-31，只允许数字，默认 1，非法/越界时提示错误）
  - 「左移 <<」按钮：`(value << n) >>> 0`
  - 「右移 >>」按钮：逻辑右移 `value >>> n`
- 操作通过现有 `updateAll` 同步更新十进制/十六进制/二进制三输入及 bit 视图。
- 后按用户要求调整布局：将「位运算」从独立卡片改为嵌入 32-bit Binary 卡片内部底部（分组标签 Byte 3-0 之后，border-t 分隔；左侧标题+说明，右侧 ml-auto 控件）。逻辑代码未变。
- 再按用户要求精简：删除「位运算」标题与说明文字，仅保留「移位位数」label + 输入框 + 左移/右移按钮一行（靠左，gap-2）；控件按 docs/rule/ui-rules.md 规范修正：按钮图标用 size-4、图标+文字显式 gap-1.5、表单组 gap-2、label 用 text-xs font-medium text-muted-foreground。
- 验证：tsc typecheck 通过（该文件无错误）；electron-vite build 成功（2405 模块）；9 组边界值移位逻辑测试全部 PASS。
- 注意：`npm run build` 直接跑会因 WorkBuddy safe-delete 拦截 vite 清空 out/ 目录而失败；需用 `npx electron-vite build --outDir <新目录>` 验证，且输出会拆分到根目录与 src/ 下（renderer），验证后需清理两处临时目录。
- 历史遗留问题（与本次无关）：flash InfoPanel formatKb 未定义、monitor ChannelPanel Set 迭代 TS2802、flash.store showReloadDialog TS2353。
